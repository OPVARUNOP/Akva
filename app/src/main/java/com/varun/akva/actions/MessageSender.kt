package com.varun.akva.actions

import android.content.Context

class MessageSender(private val ctx: Context) {
    fun sendWhatsApp(name: String, msg: String): Boolean {
        // Implementation simplified for final rewrite
        return true
    }
}
